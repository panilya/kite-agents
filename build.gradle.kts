// Root project — aggregates the multi-module build but produces no artifacts itself.
// Per-module configuration lives in each module's build.gradle.kts via the
// kite.java-library-conventions plugin in buildSrc/.

group = "io.kite"
version = "0.1.0-SNAPSHOT"
